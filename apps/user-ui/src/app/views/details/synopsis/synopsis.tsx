import React from 'react';

import './synopsis.css';

/* eslint-disable-next-line */
export interface SynopsisProps {}

export const Synopsis = (props: SynopsisProps) => {
  return (
    <div>
      <h1>Welcome to Synopsis component!</h1>
    </div>
  );
};

export default Synopsis;
